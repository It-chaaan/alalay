import type { ComponentType, ReactNode } from 'react';
import { ChevronRight } from 'lucide-react-native';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { FinancialScreenHeader } from '@/components/financial-screen-header';
import { useAppTheme } from '@/theme/theme';
import { GlassSurface } from '@/components/glass-surface';

export const settingsPalette = { background: '#F4F7F1', surface: '#FFFFFF', ink: '#11231C', muted: '#5D6C65', accent: '#0F8A6B', accentPale: '#D8EFE2', line: '#DCE8E0', danger: '#B42318' };
export function useSettingsStyles() {
  const { colors } = useAppTheme();
  return StyleSheet.create({
  safe: { flex: 1, backgroundColor: colors.background }, content: { padding: 20, paddingBottom: 40 }, loadingState: { minHeight: 220, alignItems: 'center', justifyContent: 'center', gap: 10 }, stateText: { color: colors.textSecondary, fontSize: 13 }, retry: { color: colors.primary, fontSize: 13, fontWeight: '900' },
  profile: { alignItems: 'center', paddingVertical: 12 }, avatar: { width: 92, height: 92, borderRadius: 46 }, avatarFallback: { width: 92, height: 92, alignItems: 'center', justifyContent: 'center', borderRadius: 46, backgroundColor: colors.primary }, initials: { color: colors.textOnPrimary, fontSize: 28, fontWeight: '900' }, profileName: { marginTop: 12, color: colors.textPrimary, fontSize: 22, fontWeight: '900' }, profileEmail: { marginTop: 4, maxWidth: '90%', color: colors.textSecondary, fontSize: 13, textAlign: 'center' }, editButton: { marginTop: 13, paddingHorizontal: 16, paddingVertical: 9, borderRadius: 18, backgroundColor: colors.primarySoft }, editText: { color: colors.primary, fontSize: 12, fontWeight: '900' }, section: { marginTop: 22 }, sectionLabel: { marginBottom: 8, color: colors.textSecondary, fontSize: 10, fontWeight: '900', letterSpacing: 1.2 }, group: { overflow: 'hidden', borderRadius: 17, backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border }, row: { minHeight: 62, flexDirection: 'row', alignItems: 'center', paddingHorizontal: 14, borderBottomWidth: 1, borderBottomColor: colors.border }, rowLast: { borderBottomWidth: 0 }, icon: { width: 34, height: 34, alignItems: 'center', justifyContent: 'center', borderRadius: 11, backgroundColor: colors.primarySoft }, rowCopy: { flex: 1, marginLeft: 11 }, rowLabel: { color: colors.textPrimary, fontSize: 14, fontWeight: '800' }, rowValue: { marginTop: 3, color: colors.textSecondary, fontSize: 11 }, chevron: { marginLeft: 8 }, logout: { minHeight: 50, alignItems: 'center', justifyContent: 'center', marginTop: 28, borderRadius: 25, borderWidth: 1, borderColor: colors.danger + '55', backgroundColor: colors.danger + '12' }, logoutText: { color: colors.danger, fontWeight: '900' }, card: { padding: 18, borderRadius: 18, backgroundColor: colors.surface, borderWidth: 1, borderColor: colors.border }, copy: { color: colors.textSecondary, fontSize: 13, lineHeight: 20 }, formLabel: { marginBottom: 7, color: colors.textPrimary, fontSize: 12, fontWeight: '900' }, input: { minHeight: 50, paddingHorizontal: 14, borderRadius: 13, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.surfaceInput, color: colors.textPrimary, fontSize: 14 }, field: { marginTop: 16 }, button: { minHeight: 50, alignItems: 'center', justifyContent: 'center', marginTop: 20, borderRadius: 25, backgroundColor: colors.primary }, buttonText: { color: colors.textOnPrimary, fontWeight: '900' }, error: { marginTop: 12, color: colors.danger, fontSize: 12, fontWeight: '700' }, success: { marginTop: 12, color: colors.success, fontSize: 12, fontWeight: '700' }, backNote: { marginTop: 12, color: colors.textSecondary, fontSize: 12, lineHeight: 18 }, toggleRow: { minHeight: 78, flexDirection: 'row', alignItems: 'center', paddingHorizontal: 14, borderBottomWidth: 1, borderBottomColor: colors.border }, toggleCopy: { flex: 1, marginRight: 12 }, switch: { transform: [{ scaleX: 0.9 }, { scaleY: 0.9 }] }, subGroup: { marginTop: 18 }, status: { color: colors.primary, fontSize: 12, fontWeight: '900' }, option: { minHeight: 58, flexDirection: 'row', alignItems: 'center', justifyContent: 'space-between', paddingHorizontal: 15, borderBottomWidth: 1, borderBottomColor: colors.border }, optionSelected: { backgroundColor: colors.primarySoft }, optionText: { color: colors.textPrimary, fontSize: 14, fontWeight: '800' }, muted: { color: colors.textSecondary, fontSize: 12, lineHeight: 18 }, }); }

export function SettingsHeader({ title, onBack, rightAction }: { title: string; onBack: () => void; rightAction?: ReactNode }) { return <FinancialScreenHeader title={title} onBack={onBack} rightAction={rightAction} />; }
export function SettingsSection({ title, children }: { title: string; children: ReactNode }) { const { colors } = useAppTheme(); const styles = useSettingsStyles(); return <View style={styles.section}><Text style={styles.sectionLabel}>{title}</Text><GlassSurface style={[styles.group, { backgroundColor: 'transparent', borderColor: colors.border }]} padding={0}>{children}</GlassSurface></View>; }
export function SettingsRow({ icon: Icon, label, value, onPress, last = false }: { icon: ComponentType<{ size?: number; color?: string }>; label: string; value?: string; onPress?: () => void; last?: boolean }) { const { colors, preference } = useAppTheme(); const styles = useSettingsStyles(); const appearanceValue = preference === 'system' ? 'System' : preference === 'dark' ? 'Dark' : 'Light'; const displayValue = label === 'Appearance' ? appearanceValue : value; const content = <><View style={styles.icon}><Icon size={18} color={colors.accent} /></View><View style={styles.rowCopy}><Text style={styles.rowLabel}>{label}</Text>{displayValue ? <Text style={styles.rowValue}>{displayValue}</Text> : null}</View>{onPress ? <ChevronRight size={18} color={colors.muted} style={styles.chevron} /> : null}</>; return onPress ? <Pressable accessibilityRole="button" onPress={onPress} style={[styles.row, last && styles.rowLast]}>{content}</Pressable> : <View style={[styles.row, last && styles.rowLast]}>{content}</View>; }
