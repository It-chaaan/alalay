import { useState, type FormEvent } from "react";
import { ArrowLeft, CheckCircle2, Send } from "lucide-react";
import { Container } from "../../components/layout/Container";
import { MarketingFooter } from "../../components/layout/MarketingFooter";
import { Navbar } from "../../components/navigation/Navbar";
import { getSupabaseClient } from "../../lib/supabase";

export function ContactPage() {
  const [status, setStatus] = useState<"idle" | "sending" | "success" | "error">("idle");
  const [errorMessage, setErrorMessage] = useState("");
  async function handleSubmit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault(); setStatus("sending"); setErrorMessage("");
    const form = new FormData(event.currentTarget); const supabase = getSupabaseClient();
    if (!supabase) { setStatus("error"); setErrorMessage("Contact submissions are not configured yet. Please try again later."); return; }
    const { error } = await supabase.from("contact_submissions").insert({ name: String(form.get("name") || "").trim(), email: String(form.get("email") || "").trim(), category: String(form.get("category") || "General inquiry"), message: String(form.get("message") || "").trim() });
    if (error) { setStatus("error"); setErrorMessage("We couldn't save your message right now. Please try again."); return; }
    event.currentTarget.reset(); setStatus("success");
  }
  return <main className="min-h-screen bg-[#f8f7f2] text-slate-900 dark:bg-[#111a17] dark:text-slate-100"><Navbar /><Container className="py-16 sm:py-24"><a href="/" className="inline-flex items-center gap-2 text-sm font-semibold text-[#0f8a6b] hover:text-[#0b7359]"><ArrowLeft className="h-4 w-4" /> Back to Alalay</a><div className="mt-10 grid gap-12 lg:grid-cols-[0.8fr_1.2fr] lg:gap-20"><div><p className="text-sm font-semibold uppercase tracking-[0.18em] text-[#0f8a6b]">We’re here to help</p><h1 className="mt-3 text-4xl font-semibold tracking-tight text-slate-950 dark:text-slate-100 sm:text-5xl">Contact Alalay</h1><p className="mt-5 text-lg leading-8 text-slate-600 dark:text-slate-300">Have a question, found something that feels off, or want to share an idea? Send us a note and we’ll get back to you soon.</p><div className="mt-8 rounded-2xl border border-black/5 bg-white p-5 shadow-sm dark:border-white/10 dark:bg-[#17231f]"><p className="text-sm font-semibold text-slate-950 dark:text-slate-100">Support note</p><p className="mt-2 text-sm leading-6 text-slate-600 dark:text-slate-400">Email delivery isn’t wired up yet. Messages are saved securely for the team to review in the database.</p></div></div><form onSubmit={handleSubmit} className="rounded-[2rem] border border-black/5 bg-white p-6 shadow-sm dark:border-white/10 dark:bg-[#17231f] sm:p-8"><div className="grid gap-5 sm:grid-cols-2"><label className="text-sm font-semibold text-slate-700 dark:text-slate-200">Name<input required name="name" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-[#0f8a6b] focus:ring-2 focus:ring-[#0f8a6b]/20" /></label><label className="text-sm font-semibold text-slate-700 dark:text-slate-200">Email<input required type="email" name="email" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-[#0f8a6b] focus:ring-2 focus:ring-[#0f8a6b]/20" /></label></div><label className="mt-5 block text-sm font-semibold text-slate-700 dark:text-slate-200">Subject / category<select name="category" className="mt-2 w-full rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-[#0f8a6b] focus:ring-2 focus:ring-[#0f8a6b]/20"><option>General inquiry</option><option>Bug report</option><option>Billing question</option><option>Other</option></select></label><label className="mt-5 block text-sm font-semibold text-slate-700 dark:text-slate-200">Message<textarea required name="message" rows={6} className="mt-2 w-full resize-y rounded-xl border border-slate-200 px-4 py-3 font-normal outline-none focus:border-[#0f8a6b] focus:ring-2 focus:ring-[#0f8a6b]/20" /></label>{status === "success" && <p className="mt-5 flex items-center gap-2 text-sm font-semibold text-emerald-700 dark:text-emerald-300"><CheckCircle2 className="h-5 w-5" /> Thanks — we&apos;ll get back to you soon.</p>}{status === "error" && <p className="mt-5 text-sm font-semibold text-red-600 dark:text-red-300">{errorMessage}</p>}<button type="submit" disabled={status === "sending"} className="mt-6 inline-flex min-h-12 items-center justify-center gap-2 rounded-full bg-[#0f8a6b] px-6 py-3 font-semibold text-white transition hover:bg-[#0b7359] disabled:cursor-wait disabled:opacity-60"><Send className="h-4 w-4" /> {status === "sending" ? "Sending…" : "Send message"}</button></form></div></Container><MarketingFooter /></main>;
}
